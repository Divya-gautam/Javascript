"use strict"; //use for apply new version of javascript 

// alert(3+3)  // it will not give an output because we r in node js not in browser
 let name= "Divya"
 let age = 21
 let IsLoggedIn=true
  console.log(typeof IsLoggedIn);
  console.log(typeof null);